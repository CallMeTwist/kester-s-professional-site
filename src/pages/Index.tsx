import App from "@/App";

// This is the main entry page.
// Routing is handled by the router in main.tsx.
// Index simply renders the full portfolio.

const Index = () => <App />;

export default Index;
